import {Dimensions} from 'react-native'

// -----------------------------------------------------------
// DIMENSIONS
export const wSC = Dimensions.get('window').width;
export const hSC = Dimensions.get('window').height;

// NOMBRE DE COLONES pour la liste
export const numColumns = 2;